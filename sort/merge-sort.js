const unsorted = [8, 9, 5, 3, 4, 7, 6, 1];

let lb = 0;
let ub = unsorted.length - 1;

const merge_sort = (unsorted, lb, ub) => {

    if (unsorted.length == 1) {
        return unsorted;
    }

    let mid = unsorted.length - 1;

}

const merge = () => {

}

merge_sort(unsorted);