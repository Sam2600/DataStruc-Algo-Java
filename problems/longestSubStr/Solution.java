package problems.longestSubStr;

@SuppressWarnings("unused")
public class Solution {

   public void substr(String s)
   {
      char[] char_arr = s.toCharArray();
      char[] ans_arr = new char[char_arr.length];

      for (int i = 0; i < char_arr.length; i++) {
         
      }
   }
}